name: Build and Deploy (FastAPI)

on:
  push:
    branches: [ main ]
    paths:
      - 'rfgo-vsto-fastapi/**'

jobs:
  build:
    runs-on: k8s-arc-runner 

    steps:
    - uses: actions/checkout@v4

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3
      with:
        driver: kubernetes

    - name: Login to Harbor
      uses: docker/login-action@v3
      with:
        registry: harbor.internal
        username: ${{ secrets.HARBOR_USERNAME }}
        password: ${{ secrets.HARBOR_PASSWORD }}

    - name: Build and push
      uses: docker/build-push-action@v5
      with:
        context: ./rfgo-vsto-fastapi
        push: true
        tags: harbor.foundrymtc.samsungds.net/refgo/rfgo-vsto-fastapi:${{ github.sha }}
        build-args: |
          PIP_INDEX_URL=${{ secrets.INTERNAL_PYPI_MIRROR }}
          PIP_TRUSTED_HOST=${{ secrets.INTERNAL_PYPI_TRUSTED_HOST }}

    - name: Update Image Tag
      run: |
        sed -i 's/tag: .*/tag: "${{ github.sha }}"/' rfgo-deploy/values/services/fastapi.yaml
        
    - name: Commit and Push Manifest Change
      run: |
        git config --global user.name "GHA Runner"
        git config --global user.email "gha@internal"
        git add rfgo-deploy/values/services/fastapi.yaml
        git commit -m "chore(deploy): update fastapi image to ${{ github.sha }}"
        git push origin main
