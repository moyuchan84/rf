version: '3.8'

services:
  # 1. PostgreSQL Database with pgvector
  # RAG 검색을 위해 일반 postgres 이미지를 pgvector가 포함된 이미지로 교체합니다.
  rfgo-db:
    image: ankane/pgvector:v0.5.1 # pgvector 확장 기능이 사전 설치된 이미지
    container_name: rfgo-db
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: password123
      POSTGRES_DB: photokey_db
    ports:
      - "5433:5432"
    volumes:
      - rfgo-db-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U admin -d photokey_db"]
      interval: 5s
      timeout: 5s
      retries: 5

  # 2. Redis for Pub/Sub (New)
  # vsto-fastapi와 embedding-worker 사이의 비동기 통신을 위한 메시지 브로커입니다.
  rfgo-redis:
    image: redis:7-alpine
    container_name: rfgo-redis
    ports:
      - "6379:6379"
    restart: always

volumes:
  rfgo-db-data:
  # # 2. NestJS API (Primary Backend)
  # rfgo-nestjs-api:
  #   build:
  #     context: ./rfgo-web-nestjs
  #     dockerfile: Dockerfile
  #   container_name: rfgo-nestjs-api
  #   depends_on:
  #     - rfgo-db
  #   environment:
  #     DATABASE_URL: "postgresql://admin:password123@rfgo-db:5432/photokey_db"
  #     NODE_ENV: production
  #   ports:
  #     - "3000:3000"

  # # 3. FastAPI Service (VSTO/Python Backend)
  # rfgo-fastapi:
  #   build:
  #     context: ./rfgo-vsto-fastapi
  #     dockerfile: Dockerfile
  #   container_name: rfgo-fastapi
  #   depends_on:
  #     - rfgo-db
  #   environment:
  #     DATABASE_URL: "postgresql://admin:password123@rfgo-db:5432/photokey_db"
  #     DB_USER: admin
  #     DB_PASSWORD: password123
  #     DB_HOST: rfgo-db
  #     DB_PORT: 5432
  #     DB_NAME: photokey_db
  #   ports:
  #     - "8000:8000"

  # # 4. React Web Application (Frontend)
  # rfgo-web-app:
  #   build:
  #     context: ./rfgo-web-react
  #     dockerfile: Dockerfile
  #   container_name: rfgo-web-app
  #   depends_on:
  #     - rfgo-nestjs-api
  #   ports:
  #     - "80:80"


