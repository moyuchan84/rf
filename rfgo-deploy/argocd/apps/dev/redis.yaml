apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: rfgo-redis-dev
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/refgo/rfgo-deploy.git'
    targetRevision: main
    path: charts/rfgo-redis
    helm:
      valueFiles:
        - '../../values/services/common/redis.yaml'
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: refgo-dev
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
