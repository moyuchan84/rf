apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: rfgo-fastapi-prod
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/refgo/rfgo-deploy.git'
    targetRevision: main
    path: charts/rfgo-service
    helm:
      valueFiles:
        - '../../values/env/prod.yaml'
        - '../../values/services/common/fastapi.yaml'
        - '../../values/services/prod/fastapi.yaml'
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: refgo-prod
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
